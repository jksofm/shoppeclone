export const sortBy: {
  createdAt: string
  view: string
  sold: string
  price: string
} = {
  createdAt: 'createdAt',
  view: 'view',
  sold: 'sold',
  price: 'price'
}
export const order: {
  increase: string
  descrese: string
} = {
  increase: 'asc',
  descrese: 'desc'
}
